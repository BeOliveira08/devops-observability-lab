{
  "title": "Monitoring App - Overview",
  "panels": [
    {
      "type": "graph",
      "title": "HTTP Requests Total",
      "targets": [
        {
          "expr": "http_requests_total",
          "legendFormat": "{{endpoint}}"
        }
      ]
    },
    {
      "type": "graph",
      "title": "Request Latency",
      "targets": [
        {
          "expr": "http_request_latency_seconds_bucket",
          "legendFormat": "{{le}}"
        }
      ]
    }
  ],
  "schemaVersion": 30,
  "version": 1
}
